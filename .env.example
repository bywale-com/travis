# Travis server secrets — never ship to the client
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/postgres
CURSOR_API_KEY=
# Optional: used only by npm run db:seed to fill agent_binding.cursor_agent_id
SEED_CURSOR_AGENT_ID=
