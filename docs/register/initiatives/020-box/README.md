# 020 — Travis gets a box

**PR:** [#117](https://github.com/bywale-com/travis/pull/117).  
**Envelope:** [`../../ENVELOPE-TRAVIS-BOX.md`](../../ENVELOPE-TRAVIS-BOX.md)

Founder pulled POSIX-later in. First slice planted on this PR: Fly Sprites HTTP, pointer in env (`SPRITES_TOKEN`, name defaults to `travis`), tools `run_box` / `read_box` / `write_box`. No table. Token after. Same machine on error — not a new ticket.

SA still ascribes owner / `os_node`↔disk / 042 if they write packet **020**. Cousin SCP-019 (#118) signed pass-on only — already planted as 071.
