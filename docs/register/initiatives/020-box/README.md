# 020 — Travis gets a box

**PR:** [#117](https://github.com/bywale-com/travis/pull/117).  
**Envelope:** [`../../ENVELOPE-TRAVIS-BOX.md`](../../ENVELOPE-TRAVIS-BOX.md)

Founder pulled POSIX-later in: Travis owns a Linux machine so an error retries on the same files, Tasklet-shaped. Phone stays Vercel. Seat computers stay Cursor. SA ascribes the port, the pointer, and how `os_node` meets the disk. Packet **020** if they write one.

Engineer does not plant a VM from this folder.
