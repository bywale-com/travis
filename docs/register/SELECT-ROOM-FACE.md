# Select room — FACE (considering)

**Status:** Considering. Not a packet. Photo: **L1**.  
**Job:** First land on Travis — **select a room that exists.** Then you are in that room (current voice/log).  
**Sits on:** 003 look. Does **not** recut C3/C4/D3/E1.  
**Log:** [`PHASE-ONE-LOG.md`](./PHASE-ONE-LOG.md)  
**Vision:** 2026-08-28 02:11 — rooms as primitive. This plate is the immediate glass of that.

---

## Founder wording (this pass)

- Critically, the plate to generate now: the area where you **select the room you want** — **select the room that exists**.
- First place you are supposed to **land** when you get onto Travis.

---

## Plate (one)

| ID | File | State |
|----|------|--------|
| **L1** | [`plates/travis-l1-select-room.png`](./plates/travis-l1-select-room.png) | First land · living index of **existing** rooms · Travis selected · second room quieter |

No session chrome on this screen (no `Room · via …`, no live pill, no End session, no orb). Those belong **inside** a room after you pick.

**Second row title (`Om`) and “another codebase”** are scenery so “select among existing” is visible. Not a product name lock.

---

## Glass law (draft — not locked)

**One job:** pick an existing room. Tap the row → enter that room (today’s Mode A).

Quiet selected (wash / weight), not a ring on every row. Name + one muted line (which codebase). No create. No assemble. No search. No filter grid.

**Does not:** Travis-as-agent, adding agents, minting rooms from this PNG.

---

## Five buckets (draft)

| Bucket | Draft |
|--------|--------|
| **1. Copy** | First land = list of rooms that exist. Tap enters. |
| **2. Do not build** | Bezel. `Om` / “another codebase” as product copy. Create-room CTA. Session pills on this screen. Orb. Recutting C/D/E. Hub scenery. |
| **3. Implied** | Open session after pick. One-room list still works. Last-used can be the quiet selected. |
| **4. Completes** | Empty (no rooms) as muted line, not a fake index. Return here if you leave the room (End). |
| **5. Out of scope** | Assembling a room. Travis-as-agent. Voice switch of rooms. Changing 002–005 inside-room law. |

---

## Open

- Like **L1** enough to packet?
- After pick: still Mode A voice (002), not a new inside-room?
- End session returns here?
