# Envelope — Travis Here (Engineer → SA)

**Kind:** Envelope. **Not a packet. Not a hotfix.**  
**Seat:** Engineer. Founder locked 1–4. Engineer plants the window on stood-up stores. **SA signs the Story or names a silence.** Do not append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG.  
**When:** 2026-09-04. Lived room `0e8875f8-283b-4dae-bf54-76c82a05b6ef` (seq 542–561): Travis said the backlog was empty while five open tickets sat in the same room.  
**One PR:** this branch. Packet lands here if you cut one.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25. Do not overwrite founder wording.

---

## What the founder locked

Travis is too blind. The room is its environment. It can act and still not be here. Fault-tolerant means the room stays true when it forgets to ask, searches with the wrong word, or Live is twenty minutes old.

Four options, all of them:

| # | Lock | Meaning |
|---|------|---------|
| H1 | **One Here block, pushed.** | Dest, roster idle/busy, N in motion, open backlog titles, last founder line, last few Travis lines. Unasked. Tools open one thing. |
| H2 | **Keep Live current.** | After a send, a stamp, or a tool, refresh Here into the live session. Do not freeze at connect. |
| H3 | **Let it see itself.** | Keep the last few claims, not only the last sentence. |
| H4 | **Depth only.** | Here is the look. `list_initiatives` / `search_room` never get to contradict it. A miss is no match, never “the room is empty.” |

Do not mint a table for this. 038 / 048 / 068 already taught: push unasked, bounded.

---

## Stood up (do not remint)

| Grain | Where |
|-------|--------|
| Dest | `voice_session.active_binding_id` → `agent_binding.label` |
| Roster | `room_membership` open + `seat_live_run` for busy |
| In motion | `motion` open count (Voice “N in motion” — Travis processes only) |
| Backlog titles | `initiative` open, same room |
| Turns | `voice_turn` window (038) |
| Request log | `kind=user` (048) |
| Live refresh | `session.update` with Here after token / transcript / tool. Not a store. |

Engineer plant on this PR: `hereBlock` + `TRAVIS_KEEP=3` + Live `here` on token/tool/transcript + depth-only misses + TRAVIS_SYSTEM.

---

## What SA must cut (or refuse)

No leftover analysis. Either:

1. **Sign:** Story must — the environment is pushed; tools are depth. No new table. This envelope + the plant is enough. Stamp Current. Packet number **016** if you write one; do not reuse.

2. **Name a missing grain** if dest / roster / Here needs a port or field this plant invented. Do not invent a snapshot table unless Story bears it.

3. **Named silence** (already): POSIX / unfold, cross-room look, computer use, `link` kind.

Do not collapse 012 / 013 / 015. Do not attach a protocol at create.

---

## Must-not

- A second PR because SA sat.
- A `travis.environment` / snapshot table from a picture of the window.
- Dumping seat bodies or founding lines into Here.
- Auto-seed house from migrate.
