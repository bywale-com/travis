# Envelope — Travis pass-on (Engineer → SA)

**Kind:** Envelope. **Not a packet.**  
**Seat:** Engineer. Founder: bundle the named ticket and send it; last spoken line is not a new initiative; SA must actually receive it. **SA signs the Story or names a silence.**  
**When:** 2026-09-04. Lived room `0e8875f8` seq 635–640.  
**One PR:** this branch. Next packet number is **019** if you write one.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25.

---

## Lived

| Claim | Store |
|-------|--------|
| Bundle That’s fine. + the addition, send to SA | New ticket `Yes, that's the exact one.` from seq 638. That’s fine. untouched. |
| “Calling the SA…” | No `user` turn to `sa`. No `seat_live_run`. No new `agent_binding`. No receipt. |

`ensureViaTravis` founds on the latest dest-Travis line. That is the last thing they said, not the ticket they named.

Role dest with nobody seated calls `Agent.create`. A throw writes no person and, until this cut, no receipt.

---

## Stood up (do not remint)

008 via_travis. 015 sit / role dest. 069 receipt. 070 glance.

Engineer plant: `ticketForHand(id)` stamps the addition; throws are receipts; catalog idle fallback is not a sit.

---

## What SA must cut (or refuse)

1. **Sign:** Story must — a pass-on to a named ticket does not mint a cousin from the last spoken line. A failed leave is a receipt. Catalog fallback (no sit) if spin dies. Packet **019** if you write one.

2. **Name a missing grain** if via_travis founding should never be dest-Travis chatter, or if `Agent.create` needs a port.

3. **Named silence:** auto-sit catalog slugs, computer use, Travis seeing Cursor PRs.

Do not open a second PR. Do not send **That’s fine.** from this seat.
