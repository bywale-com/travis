# src/ — where Engineer plants

| Path | Job |
|------|-----|
| `src/app/api/` | HTTP doors. Session, agents, os, artifacts, operator. |
| `src/components/` | Product face. `Room.tsx`, composer, `plates/`. |
| `src/lib/` | Pure grain. Tests `*.test.ts` live here. |
| `src/server/` | Machine: pipe, tools, queue, membership, house, motion. |
| `src/server/db/` | `schema.ts`, `migrate.ts`. SA quotes this. Engineer does not mint a table. |
| `src/theme/` | Tokens. Light and dark. No hex in components. |
| `src/surfaces/` | Surface registry boundaries. |

Do not put plates or seat logs in `src/`. Do not hard-code demo rows.
