# Rudiments

Token-driven face. Surface registry on every region. Overlay menus portal. Parametric elimination: one job per surface. How/flows before process language. Copy the cursor-rules from this pack into `.cursor/rules` when the workspace stands up. Do not hardcode theme hex. Do not invent list type sizes.
