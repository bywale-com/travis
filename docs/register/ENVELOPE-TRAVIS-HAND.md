# Envelope — Travis hand (Engineer → SA)

**Kind:** Envelope. **Not a packet.**  
**Seat:** Engineer. Founder locked: complete **That’s fine.** *inside Travis only*. Engineer plants the pipe so a handoff can be true. **SA signs the Story or names a silence.** Do not append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG.  
**When:** 2026-09-04. Lived room `0e8875f8-283b-4dae-bf54-76c82a05b6ef` (seq 542–581).  
**One PR:** this branch. Packet lands here if you cut one. Next packet number is **017** (016 is Here). Do not reuse.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25. Do not overwrite founder wording.

---

## What the founder locked

Do not complete the ticket from this Engineer bind. Plant so Travis can. Envelope so SA can sign or change a grain.

The ticket stays in the room: look at the face of the app — every control the founder can touch — and ask why Travis cannot do that; then the obvious verbs (delete / complete / …). That work leaves through Travis, or it does not leave.

---

## Lived (quote the store, not the speech)

| Claim (Travis) | Store |
|----------------|--------|
| “Starting the SA…” / “That’s in motion” | No `user` turn to `sa` after 2026-09-03. No `seat_live_run`. No queue. |
| Read SA as the cost-panel essay | Last `sa` `agent_post` is 2026-09-03, not on **That’s fine.** (`e6e01962-7c02-4b29-b28b-f507fa3e641f`) |
| “Resend … still in progress” | Dest Travis. Catalog `sa` `protocol_path` empty (created, not seated). Roster idle. |

Catalog `sa` / `pm` / `engineer` are **not seated**. Role dest must spin + sit. Do not auto-sit those slugs (015 lived smoke).

---

## Stood up (do not remint)

| Grain | Where |
|-------|--------|
| Role dest idle / spin | SCP-015 `protocol_path` + `sit_agent` + `resolveRoleDest` |
| Receipt strings | 037 `sendReceipt` / `dispatchReceipt` |
| Here | 016 `hereBlock` + Live refresh |
| Ticket posts | `voice_turn.initiative_id` + `read_initiative` |
| Live run | `seat_live_run` |
| Narration | 041 `narrateToolCall` — now a call, not a start |

Engineer plant on this PR: receipt posted to the log; ticket-scoped `read_seat_reply`; Here names not-seated and no-run.

---

## What SA must cut (or refuse)

1. **Sign:** Story must — a handoff is a receipt + a `user` turn (or a named failure). Last-by-slug is not this ticket. No seat running means not waiting. No new table. Stamp Current. Packet **017** if you write one.

2. **Name a missing grain** if dest / sit / Cursor `run_started` needs a port this plant invented. The 19:09 call left **no** `user` turn — if that is a Cursor contract hole, ascribe it. Do not mint a snapshot table.

3. **Named silence (already):** computer use (Travis looking at the face itself), auto-sit catalog slugs, Travis seeing Cursor PRs / dashboard, auto-wake on envelope, `link` / founder upload.

Do not collapse 015 / 016. Do not open a second PR because you sat. #112 already merged Here; this is a new cut.

---

## Must-not

- Complete or send **That’s fine.** from SA or Engineer outside the room.
- A `travis.environment` / handoff table from a picture of the wait.
- Auto-sit `pm` / `sa` / `engineer`.
- Cousin of #113 as the door for this grain.
