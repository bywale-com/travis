# Plates — Motion on the Log (talk + tape)

**Kind:** Printed plates. **Not a packet. Not a plant.**  
**Seat:** Engineer. Founder: Travis cannot be the bottleneck. Speech first, card under that line, interrupt while it runs, BA stays a turn.  
**When:** 2026-09-05.  
**One PR:** [#123](https://github.com/bywale-com/travis/pull/123).  
**Studio:** [`plates/studio/motion-log.html`](./plates/studio/motion-log.html). Chrome from planted Log (`Room.tsx` Talk), not the S-set.

Do not append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25.

---

## What they prove

You dump a pile. He says **Let me get to that.** The writes leave his mouth as a motion. The card sits **under** that sentence. He is Listening. You cut in; he answers; the card does not die. When the tape is done he speaks again. **Create-agent is not a step** — “Say when you want the analyst.”

`1 in motion` is the process count (P1). Thought strip stays quiet — no seat is the worker.

---

## Drawn

| ID | File | Beat |
|---|---|---|
| **M1** | [`plates/travis-m1-log-let-me-mission.png`](./plates/travis-m1-log-let-me-mission.png) | You, then Travis. No card yet. |
| **M2** | [`plates/travis-m2-log-card-collapsed-mission.png`](./plates/travis-m2-log-card-collapsed-mission.png) | Card under that line. Shut. Step 1 of 2. `1 in motion`. He has not spoken again. |
| **M3** | [`plates/travis-m3-log-card-open-mission.png`](./plates/travis-m3-log-card-open-mission.png) | Same stack, tapped. Two `write_os` rows. BA is not on the tape. |
| **M4** | [`plates/travis-m4-log-interrupt-mission.png`](./plates/travis-m4-log-interrupt-mission.png) | You interrupt. He answers. Card still step 2. Listening. |
| **M5** | [`plates/travis-m5-log-done-free-mission.png`](./plates/travis-m5-log-done-free-mission.png) | Tape done. He is free. Analyst is the next turn. |

---

## Five buckets

| Bucket | What |
|---|---|
| **1. Copy** | Speech then card then speech. Collapsed by default, tap for real steps. `1 in motion` while the tape is open. Talk footer Listening / Pause. Interrupt does not fail the card. |
| **2. Do not build** | BA inside the card. Reshape of a running step (M4 names a new path; the card does not claim it moved). Voice orb. DAG. S-set filled identity. Teaching captions. |
| **3. Implied** | Tap card toggles open. `1 in motion` opens Backlog In motion. Jump when scrolled (not this state). |
| **4. Completes** | Fail sentence + card failed. Carbon twin. Type composer (Talk is this walk). |
| **5. Out of scope** | Mutate-the-tape verb. 023 dest job. Lifting create/sit onto the queue. Planting any of this. |

---

## Sign-off

Look only. Founder names whether this is the Log glass. Engineer does not plant from the PNG.
