# Envelope — Tightness (Engineer → other eyes, then SA)

**Kind:** Envelope. **Not a packet. Not a plant.**  
**Seat:** Engineer. Founder: record the whole shape in **one** envelope so other eyes can walk it. Children live in Cursor. Protocol, management, supervisory tightness — **that is us.** The box and the ports are not enough if dest is still mail. Engineer does not mint a table from this file. Engineer does not plant a dest gate, a supervisor runner, or the connectors face from this file. Do not append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG.  
**When:** 2026-09-04 ~23:14 UTC. Lived room `0e8875f8` (title “Travis”).  
**One PR:** [#123](https://github.com/bywale-com/travis/pull/123). Next packet number is **023** if you write one for dest / supervise. **022** already signed on [#120](https://github.com/bywale-com/travis/pull/120) — do not remint `travis.port`.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25.

**Prior (do not collapse, do not remint):**  
021 worker [#119](https://github.com/bywale-com/travis/pull/119) · [`ENVELOPE-TRAVIS-WORKER.md`](./ENVELOPE-TRAVIS-WORKER.md) · [`ENVELOPE-TRAVIS-PORTS.md`](./ENVELOPE-TRAVIS-PORTS.md) + SCP-022 on [#120](https://github.com/bywale-com/travis/pull/120) · 011 · 012 · 015 · 042 · Aug 28 harness stamp (MCP named; 022 refused MCP this packet).

---

## Lived (founder wording)

Not a generated flag. Three threads, one night. The fail made them one question.

### A — Ports (we were already here)

1. Why a GitHub token **exactly:** unfold must create a repo and `git push`. Prove does not need it. 042 does not move.
2. Anything **integration-shaped** is extensibility. Stop hardcoding `TRAVIS_*` per vendor. A control: prebuilts, authorize. Travis writes into **his harness**, not this product repo. Seats write **repos**. Define primitives. He adds elements into the path that already exists.
3. Workflow: a few prebuilts (Cursor, GitHub, common). Anything new — he writes it into the harness. It appears in the connectors model. Founder taps **Authorize**. That is not a PR to `travis-psi` if the host exists.
4. Plate = **new components on a face we already have.** Those components must be generated. Voice / Log / Backlog / Create / Roster gain nothing. **I1 cannot grow.** Generate the connectors plate.

### B — The comic fail (forced the harder question)

5. Founder: *“Go ahead and deprecate the existing PM seat and create a new PM.”*
6. Travis said he would update the PM setup, then **sent it to `pm`.** Catalog `pm` was **not seated**. The old Cursor bind woke up and tried to replace itself (PRs, `SEED_CURSOR_AGENT_ID_PM`, “do not send here”).
7. It could have been **any** skill he can do. Same mistake. Thinking “sit hotfix” is too local.

### C — Supervisor (where we landed)

8. Cursor / Tasklet / Cloud have sub-agents, watch processes, tightness. Not because of computer use. Because they have a **supervisor loop**.
9. The only real difference: **sub-agents live in Cursor.** Protocol, management, supervisory tightness — **that’s us.**
10. Will one-to-four (prove, keep, unfold, hands split) stop this garbage? **No.** More verbs + dest-as-mail = more of it.

---

## Story (one shape)

Travis is a Tasklet-type product: chat plus a Linux box that works itself out. Children (PM, SA, Engineer) **stay in Cursor**. We do not spawn a second Cursor from the box. We do not lift 042 so he can sit on their disk.

What we owe — and they already have — is **tightness in our runner**:

1. **His machine** (021, planted). Prove, keep, unfold. Hands split: box ≠ seat computer.
2. **His ports** (022, signed, not planted). A control, not another env line. Host once; he fills a house manifest; founder authorizes.
3. **His dest** (not planted). He is the supervisor. Assign a scoped job, watch it, take it back. Dest is not a mailbox keyed by the noun in the sentence.

A prompt line “if it’s his, he does it” is **not** enough. That sentence already exists in other words. He still sent. The protocol has to live in the **harness**.

**Three classes, before any send**

| Class | Meaning | Move |
|-------|---------|------|
| **His** | A tool or a port he has (box, house, sit, create, prove, unfold, a wired port) | He does it. Send is refused. |
| **Theirs** | Needs that seat’s computer or protocol (essay, work-repo job) | Send / dispatch. A **job**, not a blob. |
| **Nobody here** | Cursor bind, `bc-` id, deploy, 042 | He says he cannot. Offers Engineer. Does **not** mail the noun. |

“New PM” is **his** (create + sit) or **nobody here** (rebind a Cursor id). It is never “mail the current PM.”

If he lacks a tool (no unseat, cannot mint a `bc-` id), the honest move is **name the hole**, not a send. Catalog fallback — an unseated forever-`pm` still waking so the line leaves — is part of the bug.

---

## Stood up (do not remint)

| Grain | Where |
|-------|--------|
| Box | 021 · Sprite · prove / run / read / write / unfold · `SPRITES_TOKEN` |
| Unfold key | `TRAVIS_GITHUB_TOKEN` · not wired until founder lands it · 022 hangs it on port `github` |
| House | `os_node` · 012 · not the box |
| Seats | Cursor Cloud · `cursor_agent_id` · create + sit (015) · dest never uses Travis’s path |
| 042 | He cannot see their work repo, diff, branch, CI |
| I1 | Cursor-connection chrome. Not a connectors catalog. |
| Send | `send_to_seat` / `dispatch_to_seat` · role dest · catalog fallback if nobody seated |
| Watch today | Here · `work_in_flight` · motion (his tools only; send is not a motion step) |
| 022 | Signed on #120 · `travis.port` · `/ports/<slug>.json` · paste authorize · five prebuilts · MCP/OAuth/ElevenLabs **silence** |

---

## Why 021 + 022-as-signed do not finish this

**021** makes the box a worker. Prove, house≠box, unfold. Hands-split is **computers**. Dest is still mail.

**022** makes a **list of what is his** (ports + host). The connectors plate is the founder’s hand on that list. The dest gate must **read** that list. 022 does not plant the gate. SA did not ascribe own-action vs send.

More capabilities without the gate is how the next “prove this” or “unfold that” gets mailed to a seat.

---

## Proposed walk (cohesive — not three cousins)

Children stay in Cursor. Tightness is our runner. Computer use stays back. 042 stays. No third orchestrator host.

### 1. Gate — before any send (first plant after you sign dest)

In code, not in `TRAVIS_SYSTEM`. Classify his / theirs / nobody.  
His → run the tool. Nobody → say cannot. Theirs → only then send.  
No new table. Reads the live list: declared tools + 022 ports when planted.  
Noun does not pick the mailbox.

### 2. A send is a job, not a blob

Who, one job, ticket if there is one, what “done” is. Same send / dispatch ports. Tighter args. Not a new product.

### 3. Watch is the runner we already have

Here / `work_in_flight` / motion already name a live run. Grow into “this job, this child, this state.” He owns the process. The seat owns the repo. Not a new host.

### 4. Take it back

When the child finishes, he speaks the **receipt** and stays with the founder. He does not vanish into their chat. He does not start *his* work by mailing it.

### 5. Children stay Cursor

`create_agent` + `sit_agent` is how a child appears. We do not spawn Cursor from the box. Box = his machine. Seat computer = theirs.

### 6. Ports (022 — already signed)

Plant on [#120](https://github.com/bywale-com/travis/pull/120) **after** dest is signed, or in parallel only if the gate can ship without the list (tool decls alone). Prefer: gate can ship on tools-only; ports make the list honest.

`travis.port` · house `/ports/<slug>.json` · paste Authorize · five prebuilts (`cursor`, `github`, `sprites`, `openai`, `resend`) · box exec injects env, never tees a secret · unfold prefers row then `TRAVIS_GITHUB_TOKEN`.

Two writes stay two. He does not edit this repo to add a vendor.

### 7. Holes we name, not route

No unseat. He cannot rebind a Cursor id. “Deprecate the PM” with no write → “I don’t have that,” not a send. Do not invent unseat in this envelope unless you ascribe it.

---

## Glass (PM lock — still)

Founder: new components on an existing face **are** a plate we generate.

| Face | This shape | Generate? |
|------|------------|-----------|
| I1 → **connectors** | Registry row, Authorize, empty, already-in. Prebuilt and Travis-added look the same. Replaces I1. | **Yes** (022). |
| V1 footer | Same quiet link. Label may say Connectors. | No reprint for a label. |
| Voice / Log / Backlog / V4 / roster | Dest gate is harness, not a new message kind. V4 pickers stay Cursor lists. | **No.** |
| Supervisor watch | Grows Here / in-flight / motion. Not a new app. | No new plate this envelope. |

**Do not generate:** prove, unfold, Browse OS, MCP IDE, marketplace, a plate per vendor, a “dest” screen.

---

## What other eyes should argue

- Is the three-class gate enough, or do we need a job object on day one?
- Does catalog fallback die entirely for role dest, or only when the class is his / nobody?
- Plant order: **gate first** (stops the garbage), then 022 host (honest list), then watch/take-back — or 022 first because #120 is already signed?
- Unseat this pocket, or keep naming the hole?

---

## What SA must cut (or refuse)

No leftover analysis. Packet **023** if dest / supervise needs a packet. Do not reuse 022. Do not remint `travis.port`. Same PR as **this** envelope for 023. 022 stays on [#120](https://github.com/bywale-com/travis/pull/120) unless you deliberately fold — **do not open a third PR for ports.**

1. **Sign:** Story must — children in Cursor; tightness in our runner; dest is not mail; 021+022 without the gate do not finish it. Or name a silence.

2. **Ascribe the gate** (do not leave as vibe). Where it lives (send path, not prompt). How it reads “his” (tool decls now; ports when 022 plants). What happens to catalog fallback.

3. **Ascribe job-not-blob** this packet or next. Who / job / ticket / done.

4. **Ascribe watch / take-back** this packet or next. No new host. Grow what exists or refuse.

5. **Holes:** unseat — mint, or keep as named silence. Cursor rebind stays nobody-here.

6. **022:** stands. Confirm plant order vs this gate.

7. **Must-not:** computer use; 042 lifting; third orchestrator host; marketplace; box tools becoming Cursor send; reminting 021; rewriting this product from the box; MCP this cut (022 already refused).

Do not send **That’s fine.** from Engineer. Engineer plants dest only after 023 (or a signed Story) names the gate. Engineer plants 022 only from the signed packet on #120.

---

## Live visual patterns (write-back, not a plant)

Founder seeded Gemini’s five patterns onto the live-state opportunities. Engineer write-back: [`ENGINEER-LIVE-VISUALS.md`](./ENGINEER-LIVE-VISUALS.md) — what / when / where for DAG, terminal, canvas, backlog-optimistic, seat health. Sequenced against 023 and 021/022. Does not plant a graph, PTY, canvas, or heartbeat.
