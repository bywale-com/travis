# Envelope — Prove, keep, unfold, hands split (Engineer → SA)

**Kind:** Envelope. **Not a packet. Not a plant.**  
**Seat:** Engineer. Founder: we gave him a box; be conscientious; evolve the **whole shape**, not one task. Work **all the way to four**. Keep five in the back. **SA ascribes stores and ports. SA signs the Story or names a silence.** Engineer does not mint a table from this file. Engineer does not plant prove / keep / unfold from this file. Do not append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG.  
**When:** 2026-09-04 ~22:26 UTC. Lived room `0e8875f8`. After 020 first slice on `main` ([#117](https://github.com/bywale-com/travis/pull/117)): Fly Sprite, `run_box` / `read_box` / `write_box`, `SPRITES_TOKEN` live. Smoke: he wrote `/tmp/travis-production-check.txt` and `ls` saw it.  
**One PR:** this branch. Next packet number is **021** if you write one.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25.

**Prior (do not collapse, do not remint):**  
[`ENVELOPE-TRAVIS-OS-TREE.md`](./ENVELOPE-TRAVIS-OS-TREE.md) L8 · [`ENVELOPE-TRAVIS-BOX.md`](./ENVELOPE-TRAVIS-BOX.md) · 012 house · 020 box first slice · 042 (he cannot see their work repo).

---

## Lived (founder wording)

Not a generated flag.

1. Tasklet-type product. Linux filesystem. When it errors it **works itself out**. Look at the whole shape, not That’s fine.
2. Lean to two: Travis gets a box. Engineer names externals; SA change-manages.
3. Build the first slice; founder provides the token after. Token is on Vercel. Smoke passed on the machine.
4. Nothing technically stops him testing if a **link is active**. When a process **crosses the box**, he should test that it worked; if not, that is the **retry loop**, with rules.
5. We have a tool now. Be more **conscientious**. What do we evolve next — **larger picture**, not one task.
6. **Work all the way towards four.** Keep **five** in the back. Envelope for SA: **one to four is the priority.**

---

## Priority (1–4). Five stays back.

| # | Name | Meaning |
|---|------|---------|
| **1** | **Prove** | Do → check on that same machine → retry by rule → receipt that says it worked or it failed. File exists, `curl` a public link, exit 0 — whatever the rule is. Today one `run_box` stops. The model remembering to try again is not a loop. |
| **2** | **Keep** | The Sprite disk already survives the chat. The house is still `os_node` rows. Either they become one tree, or we say they are two and stop lying. |
| **3** | **Unfold** | Stand a template into a repo. GitHub. Seats directories, not a prompt. First POSIX job named on the OS-tree envelope. |
| **4** | **Hands stay split** | Box = his computer. Cursor seats = their computers. He does not see their repo, diff, or CI unless you unlock 042. Mixing those is how the box eats the product. |
| **5** | **Browser last** | Computer use / a face. **Back. Not this priority.** Named silence unless you pull it in later. `curl` from the box is not a browser. |

Conscientious: we do not evolve by handing him the whole OS. We hang **proof and memory** on the machine we already gave him, then unfold, and we do not let his box become their Cursor machine.

---

## Stood up (do not remint)

| Grain | Where |
|-------|--------|
| Phone | Vercel · `travis-psi` |
| House | `travis.os_node` · 012 · list/read/write_os |
| Box | Fly Sprites HTTP · env `SPRITES_TOKEN` + `TRAVIS_SPRITE_NAME` · run/read/write_box · no table |
| Seats | Cursor Cloud · `cursor_agent_id` · Travis dest never uses that path |
| 042 | He cannot see a work repo, diff, branch, CI |
| Retry today | Model may call `run_box` again. **No harness loop. No verify step.** |

---

## Need side (externals)

| Beat | New external? |
|------|----------------|
| **1 Prove** | No. Same Sprite. Harness around `run_box` / `write_box`. Public `curl` is allowed from that machine. |
| **2 Keep** | Only if disk-as-truth needs object storage. Persistent Sprite already is a disk. SA says how it meets `os_node`. |
| **3 Unfold** | **GitHub.** Token server-side, same rule as `CURSOR_API_KEY`. Git on the box plus a push. |
| **4 Split** | No new system. Law. 042 stays unless SA moves it. |
| **5** | Browser / computer-use vendor. **Not this envelope.** |

Do not add: a third orchestrator host, an integrations table, a second chat, reminting 012 as a VM.

---

## What SA must cut (or refuse)

No leftover analysis. Priority is **1 → 4** in that order unless you name a reason to swap. Packet **021** if you write one. Do not reuse. Same PR.

1. **Sign:** Story must — the box is a worker with rules, not a second mouth. Prove, then keep, then unfold. Hands stay split. Five stays back.

2. **Prove — ascribe the rules** (do not leave as vibe): what counts as success (exit, file, public HTTP); how many retries; when to stop and name failure; whether verify is harness after every box write/run or only when the job crossed the box. Prefer no new table.

3. **Keep — ascribe the join:** one Travis, one box (already env). How `os_node` meets the Sprite disk — rows dump onto disk, disk is the truth, or they stay two and the Story says so.

4. **Unfold — ascribe the port:** GitHub key name; which template path; whether 042 moves when he pushes a repo. First job remains “seats directories, not a startup message.”

5. **Four — must-not:** box tools never become Cursor send. Seat computers never become his disk. No auto-sit. No computer use in 021.

6. **Named silence (back):** computer use, Travis seeing Cursor PRs / dashboard, auto-sit catalog slugs, dedicated orchestrator host.

Do not remint 012 / 020 first slice. Do not open a second PR. Do not send **That’s fine.** from Engineer. Engineer plants prove only after 021 (or a signed Story) names the rules.
