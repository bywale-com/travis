# PM packet 005 — Log text format (receive + composer grow)

**Number:** `005` — next PM packet is `006`. Never reuse. Index: [`PM-PACKETS.md`](./PM-PACKETS.md).  
**Seat:** Product Manager. **SA** ascribes render vs store, speakable text vs formatted display, composer cap. **Engineer** plants glass from the photo + this packet (after SA).  
**When:** Locked 2026-08-27 — founder: like **E1** enough to packet.  
**Photo:** [`plates/travis-e1-log-format.png`](./plates/travis-e1-log-format.png) · FACE [`LOG-FORMAT-FACE.md`](./LOG-FORMAT-FACE.md).  
**Builds on:** [`PM-PACKET-004-LOG-COMPOSER.md`](./PM-PACKET-004-LOG-COMPOSER.md) (Talk \| Type) · look [`PM-PACKET-003-LOOK-QUEUE.md`](./PM-PACKET-003-LOOK-QUEUE.md). Do **not** recut C3/C4 or D3. Do **not** rewrite 004.  
**Flag:** [`PHASE-ONE-LOG.md`](./PHASE-ONE-LOG.md) **14:00 UTC** — do not overwrite. Read: [`PLATE-READ.md`](./PLATE-READ.md).  
**Does not:** attach; send-side rich editor; image/file plant; recut C/D plates; mint tables from the PNG; Hub scenery; Mode A composer.

**The look of record is the photo. This packet focuses it.**

---

## Do not miss

**Receive is the loud job.** Agent posts in the log are **organized**, not a flat dump of `turn.text`. Grain the photo shows (lock this, not a markdown kitchen sink):

| On glass | What it is |
|----------|------------|
| Short heading | Weight, not a new chrome bar |
| Spaced list | Separate lines, not one run-on |
| Inline code | A fragment in the sentence |
| Paragraphs | Spacing so the post can be scanned |

Same 003 bubble chrome: cream + hairline, agents left, you right. **User bubbles stay plain.**

**Composer grow (Type only).** The field **expands** with wrapped text up to a **cap**, then you **scroll inside** the composer. That is the Claude behavior. D3’s single-line bar is not the plant for this pocket. Mic + send stay. No formatting toolbar. No paperclip.

E1 shows **grown** (several wrapped lines). **Max + inner scroll** is Completes — no extra PNG. Same receive format in **Talk** (no field) is Completes.

**004 still holds.** Talk | Type. `@` by title. Mic into the field. This packet does not move those objects.

**Stage, not this plant.** Images and other file types will sit in the **same thread order** as formatted text. Do not invent a gallery, attach control, or file chrome here.

### Look

Match current planted log (003 / C temperature). E1 is the photo for **this pocket’s new objects** (organized agent post, grown composer). Ignore on the PNG: bezel, keyboard as product, checkmarks, globe-on-pill if the plant already dropped it, photos-of-people, k8s / Hub copy.

D3 remains the 004 photo. Do not plant E1 as a second Talk|Type.

---

## Paste — Systems Analyst

```text
You are Travis’s Systems Analyst. Identity: docs/README.md § Systems Analyst · docs/seats/SYSTEMS-ANALYST.md. Log: docs/register/SYSTEMS-ANALYST-LOG.md. Product flag PHASE-ONE-LOG 14:00 is read-only.

Face pocket: docs/register/PM-PACKET-005-LOG-FORMAT.md + LOG-FORMAT-FACE.md + plate travis-e1-log-format.png. Builds on 004; do not rewrite 004.

Do not mint tables from the picture. Do not freeze keyboard, bezel, or checkmarks. Images/files are stage — name the silence; do not stand up a file store for this packet.

Ascribe — change packet so Engineer only cuts:

A. Agent post display: formatted subset (heading, list, inline code, paragraphs) vs raw turn.text. What is stored vs what is rendered. Do not invent a kitchen-sink markdown dialect.

B. Speakable path: TTS still reads the reply; do not speak markdown punctuation as content. Hygiene, not triage. Fit vs existing stream hygiene.

C. User turns stay plain. No send-side rich document.

D. Type composer: grow with wrap to a cap, then scroll inside the field. Cap is glass (name a rule Engineer can plant). Not Mode A.

E. Same receive format in Talk (no composer). 003 queue/barge and 004 Talk|Type still apply.

Stamp SYSTEMS-ANALYST-LOG. Cut a change packet when Engineer has no analysis left.
```

## Paste — engineer

```text
Read docs/register/PM-PACKET-005-LOG-FORMAT.md ALL THE WAY THROUGH. Then LOG-FORMAT-FACE.md and PLATE-READ.md. Photo: docs/register/plates/travis-e1-log-format.png.

You are Travis’s Engineer. Identity: docs/README.md § Engineer · AGENTS.md. Wait for SA change packet before planting the machine. Glass: match current log look; add only this pocket.

Receive: organize agent posts (heading, list, inline code, paragraphs) — not a flat string. User bubbles stay plain. Type composer grows with wrap then inner-scroll; mic + send stay. No toolbar. No attach.

004 still owns Talk | Type and @. Do not recut C3/C4/D3. Do not plant E1 as a second toggle. Ignore PNG bezel, keyboard-as-product, checkmarks. Images/files are not this plant.

Do not mint tables SA refused. Do not append PM/SA logs. Two buckets only.
```

---

## Five buckets (PLATE-READ)

1. **Copy:** Organized agent text in the log (heading, list, inline code, paragraphs). Type composer **grows** with wrap, then **scrolls inside** at a cap. User send stays plain. Log mode only.

2. **Do not build:** Formatting toolbar. Paperclip / attach. Send-side rich editor. Bezel as product. Keyboard as product. Checkmarks. Globe-on-pill if plant dropped it. Photos of people. k8s / Hub copy. Recutting C3/C4/D3. E1 as a second Talk|Type plant. Image/file gallery.

3. **Implied:** Thread still scrolls. Keyboard. Send still posts. Markdown-ish receive of that subset. TTS still plays the reply (hygiene strips markup from speech).

4. **Completes:** Composer at max + inner scroll (no extra PNG). Same receive format in Talk (no field).

5. **Out of scope this build:** Images / files as a plant (stage only). Voice-mode typing. New seats. Triage. Changing 003 queue law or 004 toggle/`@`/mic.

---

## Founder comments (preserve)

- Time to work on **text formatting** — even composer formatting.
- Claude: composer **expands** up to a **limit**, then **scroll inside**. Travis is still a **flat** composer.
- Better formatting on what comes from the **agents** (receive). Maybe not too much on what you send.
- Sets the **stage** for images and other file types — how they are **organized** in the thread.
- This justifies plates. **Like E1 enough to packet.**

---

## Do not

- Overwrite the 14:00 flag.
- Rewrite 004 (Talk | Type, `@`, composer mic).
- Put a composer or this format toolbar on Mode A.
- Plant images/files from this packet.
- Mint a store in this PM packet.
- Treat D3 as the format photo, or E1 as the toggle photo.
- Append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG from the Engineer cut.
