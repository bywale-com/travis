# Envelope — Ports as a control (Engineer → SA)

**Kind:** Envelope. **Not a packet. Not a plant.**  
**Seat:** Engineer (founder also seated this bind PM+Engineer for the glass). Founder: integration-shaped keys must stop being one-off env lines. Travis writes **his harness**, not this product repo. Seats write **repos**. **SA ascribes the store and the host. SA signs the Story or names a silence.** Engineer does not mint a table from this file. Engineer does not plant a port registry, MCP host, or connectors face from this file. Do not append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG.  
**When:** 2026-09-04 ~22:54 UTC. Lived room `0e8875f8`. After **021** on `main` ([#119](https://github.com/bywale-com/travis/pull/119)): prove / keep / unfold planted. Unfold receipts `not wired` until `TRAVIS_GITHUB_TOKEN`.  
**One PR:** this branch. Next packet number is **022** if you write one.

**Flag (unchanged):** Phase One 14:00 UTC 2026-08-25.

**Prior (do not collapse, do not remint):**  
[`ENVELOPE-TRAVIS-OS-TREE.md`](./ENVELOPE-TRAVIS-OS-TREE.md) (add-in-one-room → everywhere; I1 is Cursor chrome) · [`ENVELOPE-TRAVIS-WORKER.md`](./ENVELOPE-TRAVIS-WORKER.md) · 011 IntegrationStatus · 012 refused `travis.integration` · 021 unfold key `TRAVIS_GITHUB_TOKEN` (not `GITHUB_TOKEN`) · 042 · Aug 28 harness-vs-model stamp (MCP named; **do not plant MCP from that stamp**).

---

## Lived (founder wording)

Not a generated flag.

1. Why a GitHub token **exactly:** unfold must `POST /user/repos` and `git push`. Prove does not need it. 042 does not move.
2. Anything **integration-shaped** is extensibility. Stop hardcoding `TRAVIS_*` per vendor. A control: prebuilts, then authorize. Maybe a module. Map what we already hardcode.
3. Travis will write **into his own harness** what he needs. Travis-related (not repo-related) he handles. He does **not** rewrite his product code. Define the **primitives**. He adds elements into the path that already exists.
4. Workflow he is testing: a few prebuilts (Cursor, GitHub, common). Anything new — he writes it into the harness, maybe pushes **his** tree. It appears in the connectors model. Founder taps **Authorize**.
5. Challenge: that feels like writing code. How does it not write **this** codebase?
6. Plate = **new components on a face we already have.** Even if the face exists, those components must be generated. Rigorous pass: Voice / Log / Backlog / Create / Roster gain nothing. **I1 cannot grow.** Its job changes. Generate that plate.

---

## Story (for you to sign)

Ports are a **control**, not another env name. The product plants a **host** once. Travis fills it (manifest / tool server / authorize recipe) on the **box or house**. The phone lists whatever is registered. Founder authorizes. Secret never hits the phone.

**Two writes — do not collapse them.**

| Write | Where | Push? |
|--------|--------|--------|
| **Product** | This app (`travis-tools.ts`, a new I1 section, a new env name) | Deploy `travis-psi`. Engineer / a seat. **Not Travis-related.** |
| **Harness** | Descriptor + maybe a small server on **his box** (or a house file) | His tree if he unfolded one. **Not** this repo. |

If the host does not exist and every new vendor needs a PR here, the Story is false. That is the hardcoded path again.

**Hands stay split.** Travis writes Travis (ports, harness, prove, unfold). Seats write repos. 042 stands. Box tools never become Cursor send.

---

## Stood up (do not remint)

| Grain | Where |
|-------|--------|
| I1 | `IntegrationStatus` · titled **Cursor** · hardcoded Cursor / GitHub-through-Cursor / models / repos · “Set up” = Vercel `CURSOR_API_KEY` |
| V4 pickers | Cursor’s model + repo lists. Not his GitHub. |
| Room index door | Footer quiet **Cursor** → I1 |
| Cursor | `CURSOR_API_KEY` · seats |
| Travis mouth | `OPENAI_API_KEY` |
| Box | `SPRITES_TOKEN` · prove / run / read / write |
| Unfold | `TRAVIS_GITHUB_TOKEN` · create + push **his** new repo · not wired until founder lands it |
| Mail | `RESEND_API_KEY` |
| 011 GitHub row | Cursor can **see their** repos. Not Travis owning GitHub. |
| 012 / 021 | **Refused** `travis.integration`. Install was env. |

`DATABASE_URL` is the store. It is **not** a port.

---

## Glass (PM lock — generate after you ascribe)

Founder definition: new components on an existing face **are** a plate we generate.

| Face | This pocket | Generate? |
|------|-------------|-----------|
| I1 Cursor connection | Job becomes **the ports**. Registry row, Authorize, empty, already-in. Prebuilt and Travis-added look the same. | **Yes.** Replace I1. Do not grow the Cursor PNG. |
| V1 room index | Footer already opens I1. Label may say Connectors. Same quiet link. | No reprint for a label. |
| V4 create agent | Still Cursor lists. Do not add a second repo picker. | No |
| Voice / Log / Type / U1 / U2 | Tool receipts stay text. | No |
| V6 / Backlog | Prove already fits a motion row. Unfold does not mint a ticket. | No |
| V2 / V3 / K1 / login / queue / thought | No | No |

**On the generated plate (one job: the ports):**

- A **row**: name, connected / not, Authorize
- **Empty** (none wired)
- **Already in** (Authorize again)
- **Authorize beat** — paste token or vendor handoff. Completes, same plate, second state if you want it drawn. Keys stay server-side.

Add-a-new-port is spoken or a quiet door on this plate, not its own plate. After he writes it into the harness, the row appears for the founder to authorize.

**Do not generate:** prove, unfold, house/box tree, MCP IDE, marketplace, a plate per vendor.

Engineer generates the connectors plate **after** you ascribe the row and the authorize beat. Not from this envelope.

---

## What SA must cut (or refuse)

No leftover analysis. Packet **022** if you write one. Do not reuse. Same PR.

1. **Sign:** Story must — ports are a control; Travis writes the harness; seats write repos; a new port is not a PR to this repo. Or name a silence and why.

2. **Ascribe the registry** (do not leave as vibe). 012 / 021 refused `travis.integration`. Un-refuse that row, or name another grain (house path, box file, env-behind-the-plate). No `session_id` on a key. OS-scoped: add in one room → everywhere. `DATABASE_URL` stays out.

3. **Ascribe the host:** what he is allowed to write (manifest, MCP server, token recipe — pick or refuse); where it lives (box vs house); how the phone lists it without a product deploy. If you refuse a generic host, say that new vendors stay Engineer plants.

4. **Ascribe authorize:** paste vs OAuth this packet; where the secret lives (not the phone, not Sprite disk); what happens to `TRAVIS_GITHUB_TOKEN` and `GITHUB_TOKEN` (011). His GitHub (create/push) ≠ Cursor’s repo list.

5. **Prebuilts this packet:** which slugs. Candidates already wired as env: `cursor`, his `github`, `sprites`, `openai`, `resend`. Do not invent a marketplace of common apps. ElevenLabs stays considering unless you pull it in.

6. **Glass:** confirm the connectors plate is the one face. I1’s Cursor/models/repos dump does not remain the job. V4 pickers unchanged.

7. **Must-not:** marketplace browse; prove / unfold / Browse-OS plates; computer use; auto-sit catalog slugs; box tools becoming Cursor send; 042 lifting so he can edit `bywale-com/travis`; dumping the house onto the Sprite as one tree; reminting 021.

8. **Named silence (back):** computer use; Travis seeing Cursor PRs / dashboard; a second repo picker on V4; rewriting this product from the box.

Do not remint 011 / 012 / 020 / 021. Do not open a second PR. Do not send **That’s fine.** from Engineer. Engineer plants the host and the connectors face only after 022 (or a signed Story) names the grain.
