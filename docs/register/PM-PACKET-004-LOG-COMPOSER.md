# PM packet 004 — Log Talk/Type toggle + composer + @

**Number:** `004` — next PM packet is `005`. Never reuse. Index: [`PM-PACKETS.md`](./PM-PACKETS.md).  
**Seat:** Product Manager. **SA** ascribes composer send / `@` / log sub-mode. **Engineer** plants glass from the photo + this packet.  
**When:** Locked 2026-08-27 — founder: create the packet. D3 is fine; specify the **toggle** in writing (not “just a chip”). Two titles. Mic in the composer.  
**Photo:** [`plates/travis-d3-log-type-toggle.png`](./plates/travis-d3-log-type-toggle.png) · FACE [`LOG-COMPOSER-FACE.md`](./LOG-COMPOSER-FACE.md).  
**Builds on:** Mode B in [`PM-PACKET-002-ROOM.md`](./PM-PACKET-002-ROOM.md) · look/queue [`PM-PACKET-003-LOOK-QUEUE.md`](./PM-PACKET-003-LOOK-QUEUE.md). Do **not** recut C3/C4.  
**Flag:** [`PHASE-ONE-LOG.md`](./PHASE-ONE-LOG.md) **14:00 UTC** — do not overwrite. Read: [`PLATE-READ.md`](./PLATE-READ.md).  
**Does not:** composer on Mode A; attach; recut C plates; mint tables from the PNG; Hub scenery.

**The look of record is the photo. This packet focuses it.**

**Toggle titles (two options — write these strings):** **Talk** · **Type**

---

## Do not miss

**Log mode only.** Mode A voice is unchanged: no field, no Talk/Type control.

The control in the **room-chip row** (`Room · via {seat}` · `{seat} · live`) is a **two-state toggle**, not a third status chip like `Eng · live`. It **reveals or hides the composer and switches log sub-mode**.

### Toggle titles (locked here — two options only)

| State | Title | What log does |
|-------|--------|----------------|
| **Talk** | Talk | Current log. No composer. Speech / done-phrase as log already works. |
| **Type** | Type | Composer on. Type (or mic into the field). Send. `@` to address. |

D3 shows **Type** on. **Talk** is Completes — no extra PNG.

D3 paints **Type** as a filled pill in that row. Plant a **toggle** that uses those two titles. Do not plant a sticky third “live”-style chip with no off.

### Composer (Type only)

- Quiet field. Send icon.
- **Mic** in the composer: speech **into this box** (not Mode A orb / not Travis-as-only-voice). Different from voice mode.
- **`@`**: submodal of agents **already in the room**, by **title** (PM · SA · Engineer). Pick → chip in the field → keep writing.
- No paperclip. No attach.

### Look

Match **current planted log** (003 / C temperature). D3 is the photo for **this pocket’s new objects** (toggle, field, `@` list, mic, send). Ignore on the PNG: bezel, checkmarks, k8s copy, globe-on-pill if the plant already dropped it, photos-of-people.

D1 / D2 are extras. Do not plant D1 as a second screen.

---

## Paste — Systems Analyst

```text
You are Travis’s Systems Analyst. Identity: docs/README.md § Systems Analyst · docs/seats/SYSTEMS-ANALYST.md. Log: docs/register/SYSTEMS-ANALYST-LOG.md. Product flag PHASE-ONE-LOG 14:00 is read-only.

Face pocket: docs/register/PM-PACKET-004-LOG-COMPOSER.md + LOG-COMPOSER-FACE.md + plate travis-d3-log-type-toggle.png.

Do not mint tables from the picture. Do not freeze k8s copy or checkmarks.

Ascribe — change packet so Engineer only cuts:

A. Log sub-mode Talk | Type — session state; switch preserves one session and the log. Default when entering log (Talk vs last).

B. Type send: how a typed (or mic-filled) line becomes a user turn + Cursor send. Fit vs done-phrase conductor. @ addressee vs current via-pill vs call-by-name.

C. @ list: room seats by title only (PM SA Engineer) — not a cloud-agent directory. Chip in the field.

D. Composer mic: STT into the field (not Mode A presence). Port reuse vs silence.

E. Talk state: no composer; existing listen/finalize path still works in log.

F. Queue/barge (003) still applies to typed sends to a busy seat.

Stamp SYSTEMS-ANALYST-LOG. Cut a change packet when Engineer has no analysis left.
```

## Paste — engineer

```text
Read docs/register/PM-PACKET-004-LOG-COMPOSER.md ALL THE WAY THROUGH. Then LOG-COMPOSER-FACE.md and PLATE-READ.md. Photo: docs/register/plates/travis-d3-log-type-toggle.png.

You are Travis’s Engineer. Identity: docs/README.md § Engineer · AGENTS.md. Wait for SA change packet before planting the machine. Glass: match current log look; add only this pocket.

Mode B chip row: two-state toggle titles Talk | Type (not a third live-status chip). Type reveals composer (field, mic into field, send, @ list by title). Talk hides composer; log listen stays as now.

No composer / no toggle on Mode A. No attach. Do not recut C3/C4. Do not plant D1. Ignore PNG bezel, checkmarks, k8s copy.

Do not mint tables SA refused. Do not append PM/SA logs. Two buckets only.
```

---

## Five buckets (PLATE-READ)

1. **Copy:** Talk | Type toggle in the room-chip row. **Type:** composer, mic-into-field, send, `@` popover of room seats by title, chip then keep typing. **Talk:** no field. Log mode only.

2. **Do not build:** Toggle or composer on Mode A. Attach/paperclip. Bezel as product. Checkmarks. k8s / Hub copy. Photos of people as avatars if plant uses initials. Full-screen agent sheet. `bc-…` picker. D1 as a separate plant. Recutting C3/C4.

3. **Implied:** Toggle shows/hides the field and switches sub-mode. Keyboard. Send posts a user turn. `@` pick dismisses the list. Mic fills the field. Send with no `@` uses current via-pill.

4. **Completes:** Talk state (no extra PNG). Empty `@` cancel. Backspace the chip.

5. **Out of scope this build:** Voice-mode typing. New seats. Triage. Changing 003 queue law.

---

## Founder comments (preserve)

- Text box like a regular chat app; **log mode only**.
- `@` → agents in the room, by title → keep writing.
- Toggle: current log (no composer) vs this type way. Reveals composer **and** changes the mode. Lives **with the room chips**.
- Mic in the composer.
- D3 is fine. It’s a **toggle**, not just a chip — specify in the packet, including the **two titles**.
- Create the packet.

---

## Do not

- Overwrite the 14:00 flag.
- Put Talk/Type or the field on Mode A.
- Mint a store in this PM packet.
- Treat D1 as the plant photo.
- Append PHASE-ONE-LOG or SYSTEMS-ANALYST-LOG from the Engineer cut.
